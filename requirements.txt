# =============================================================================
# MentalTrendAI - Dependencies / Dependensi Python
# =============================================================================
# File: requirements.txt
# Deskripsi: Daftar semua package Python yang diperlukan untuk menjalankan
#            pipeline MentalTrendAI. Install dengan: pip install -r requirements.txt
#
# Versi Python: >= 3.10
# Terakhir diupdate: 2025-11-30
# =============================================================================

# -----------------------------------------------------------------------------
# LIBRARY DATA SCIENCE DASAR (Core Data Science Libraries)
# -----------------------------------------------------------------------------
# Digunakan untuk manipulasi data, operasi numerik, dan preprocessing

pandas>=2.1.0           # Manipulasi data tabular (DataFrame, read_csv, groupby)
                        # Fungsi: Load dataset, preprocessing, aggregasi data

numpy>=1.26.0           # Operasi numerik dan array multidimensi
                        # Fungsi: Operasi matematis, reshape data untuk LSTM

scikit-learn>=1.3.0     # Machine learning dan preprocessing
                        # Fungsi: train_test_split, MinMaxScaler, RobustScaler,
                        #         metrics (RMSE, MAE, accuracy_score, f1_score)

# -----------------------------------------------------------------------------
# NLP DAN ANALISIS SENTIMEN (Natural Language Processing)
# -----------------------------------------------------------------------------
# Digunakan untuk sentiment analysis dan text embedding

transformers>=4.36.0    # Hugging Face Transformers - BERT, RoBERTa models
                        # Model: cardiffnlp/twitter-roberta-base-sentiment-latest
                        # Fungsi: Analisis sentimen tweets (Positif/Negatif/Netral)

torch>=2.1.0            # PyTorch - Backend untuk Transformers
                        # Fungsi: Neural network operations, GPU acceleration

sentencepiece>=0.1.99   # Tokenizer untuk model multilingual
                        # Fungsi: Tokenisasi teks untuk BERT

accelerate>=0.25.0      # Hugging Face Accelerate - optimasi GPU
                        # Fungsi: Mempercepat inferensi model Transformers

# -----------------------------------------------------------------------------
# TOPIC MODELING (Pemodelan Topik)
# -----------------------------------------------------------------------------
# Digunakan untuk ekstraksi topik dari kumpulan dokumen

nltk>=3.8.1             # Natural Language Toolkit
                        # Fungsi: word_tokenize (tokenisasi kata),
                        #         stopwords (penghapusan kata umum),
                        #         punkt (sentence tokenization)
                        # Note: Jalankan nltk.download('punkt'), nltk.download('punkt_tab'),
                        #       nltk.download('stopwords') setelah install

# -----------------------------------------------------------------------------
# TIME SERIES CLASSIFICATION (Klasifikasi Deret Waktu)
# -----------------------------------------------------------------------------
# Digunakan untuk klasifikasi risiko kesehatan mental berbasis time series

tensorflow>=2.15.0      # TensorFlow - Deep learning framework
                        # Model: LSTM (Long Short-Term Memory)
                        # Fungsi: Klasifikasi time series risiko kesehatan mental,
                        #         Bidirectional LSTM, BatchNormalization,
                        #         Huber loss untuk robust training

tf-keras>=2.15.0        # Keras API untuk TensorFlow
                        # Fungsi: Sequential model, Dense layers,
                        #         callbacks (EarlyStopping, ReduceLROnPlateau,
                        #         ModelCheckpoint)

# -----------------------------------------------------------------------------
# VISUALISASI (Visualization Libraries)
# -----------------------------------------------------------------------------
# Digunakan untuk membuat grafik, dashboard, dan visualisasi interaktif

streamlit>=1.29.0       # Framework web app interaktif
                        # Fungsi: Dashboard MentalTrendAI,
                        #         sidebar filters, metrics display

plotly>=5.18.0          # Library grafik interaktif
                        # Fungsi: Pie chart sentimen, line chart time series,
                        #         visualisasi interaktif

matplotlib>=3.8.0       # Library plotting dasar
                        # Fungsi: Training loss plot, confusion matrix heatmap

seaborn>=0.13.0         # Statistical visualization
                        # Fungsi: Heatmap, barplot dengan color palette

wordcloud>=1.9.0        # Generator word cloud
                        # Fungsi: Visualisasi kata-kata dominan dari tweets

Pillow>=10.1.0          # Python Imaging Library
                        # Fungsi: Manipulasi gambar untuk word cloud

# -----------------------------------------------------------------------------
# UTILITAS (Utility Libraries)
# -----------------------------------------------------------------------------
# Library pendukung untuk progress tracking dan date handling

tqdm>=4.66.0            # Progress bar
                        # Fungsi: Menampilkan progress saat processing batch data,
                        #         topic assignment, sentiment analysis

python-dateutil>=2.8.2  # Utilitas tanggal dan waktu
                        # Fungsi: Parsing tanggal, timedelta operations

# =============================================================================
# CATATAN INSTALASI (Installation Notes):
# =============================================================================
#
# 1. Instalasi Dasar:
#    pip install -r requirements.txt
#
# 2. Untuk GPU (CUDA):
#    pip install torch --index-url https://download.pytorch.org/whl/cu118
#    pip install tensorflow[gpu]
#
# 3. Download NLTK Data (WAJIB setelah install):
#    python -c "import nltk; nltk.download('punkt'); nltk.download('punkt_tab'); nltk.download('stopwords')"
#
# 4. Verifikasi Instalasi:
#    python -c "import pandas, numpy, torch, tensorflow, transformers, nltk, streamlit; print('All OK!')"
#
# =============================================================================